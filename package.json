{
  "name": "text-comprehend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -b tsconfig.build.json && npm run build --workspace @text-comprehend/dashboard",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json && tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/dashboard/tsconfig.json && tsc --noEmit -p .opencode/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
